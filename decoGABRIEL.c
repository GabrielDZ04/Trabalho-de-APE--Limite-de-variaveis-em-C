#include <stdio.h> 
#include <stdlib.h>
#include <string.h> 
#include <math.h>

int main() {

        /*for (j = 0; j <=length; j+=2)
        {
            dupla[0] = hexadecimal[j];
            dupla[1] = hexadecimal[j+1];
            decimal = strtol(dupla, NULL, 16);

            if (func_val(j, b) != 0){
                printf("%c", decimal);
            }

            if(decimal == 0){
                break;
            }            
        }
        
        printf("\n \n");
    }*/

    return 0; 
}